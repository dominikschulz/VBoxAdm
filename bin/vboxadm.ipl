#!/usr/bin/perl
use strict;
use warnings;

use lib '@LIBDIR@';

use VBoxAdm::Controller::CLI '@VERSION@';

binmode( STDIN, ':utf8' );

my $CLI = VBoxAdm::Controller::CLI::->new();
$CLI->run();

exit 0;

__END__

=head1 NAME

vboxadm - Mailbox manager

=cut
