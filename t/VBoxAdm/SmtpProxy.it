use Test::More qw( no_plan );
use VBoxAdm::SmtpProxy '@VERSION@';

BEGIN { use_ok( 'VBoxAdm::SmtpProxy', '@VERSION@' ); }

# Test is_spam
{
    # TODO setup mockup objects
    #my $p = VBoxAdm::SmtpProxy->new();
    # TODO test spam
    #ok($p->is_spam(),'Is Spam');
    # TODO test ham
    #ok(!$p->is_spam(),'Is Ham');
}
# Test max_msg_size
# Test sa_block_score
# Test want_sa