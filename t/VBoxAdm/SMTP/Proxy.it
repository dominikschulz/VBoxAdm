use Test::More qw( no_plan );
use VBoxAdm::SMTP::Proxy '@VERSION@';

BEGIN { use_ok( 'VBoxAdm::SMTP::Proxy', '@VERSION@' ); }

# Test is_spam
{

    # TODO setup mockup objects
    #my $p = VBoxAdm::SMTP::Proxy->new();
    # TODO test spam
    #ok($p->is_spam(),'Is Spam');
    # TODO test ham
    #ok(!$p->is_spam(),'Is Ham');
}

# Test max_msg_size
# Test sa_block_score
# Test want_sa
