user = postfix
password = PASSWORD
hosts = localhost
dbname = vboxadm
query = SELECT CONCAT(ma.local_part,'#',d.name,'@autoreply.planet49.com') AS goto FROM mailboxes AS ma LEFT JOIN domains AS d ON ma.domain_id = d.id WHERE is_on_vacation AND d.name = '%d' AND ma.local_part = '%u'