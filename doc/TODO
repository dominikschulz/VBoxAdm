TODO:

- Evaluate alternatives to postgrey
Make sure class-C ip subnet auto-whitelists are supported

- Allow dynamic mysql lookup tables in recipient checks?

- Templates:
-- Add XML templates
-- Add CSV templates
-- Add alternate styles
-- Add plaintext templates

- test conversion tools for debmin, Vexim, Postfixadmin

- SmtpProxy:
-- Multiple target SMTP servers (fail over, load balancing)
-- Finish/Test XFORWARD handling*
-- After process_request log After-Filter Postfix Queue ID, Remote IP, Helo, Auth-Type, etc.
-- Implement own SMTP Client/Server classes*

- All:
-- Multiple database servers (fail over, load balancing)

- Vacation:
-- add vacation blacklist to list senders which never get an reply
-- Test new vacation query
-- add vacation blacklist GUI*

- RFC Notfiy:
-- finish 'you've been blocked' RFC notify cronjob*
-- test rfc notify

- AWL Cron:
-- add auto whitelist cron + DB + GUI*

- Extensions:
-- Add "customer extension"
-- Add multi-ip sendout extension

- Translation:
-- Translate status messages

* - in progress