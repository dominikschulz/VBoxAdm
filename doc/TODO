TODO:

- Evaluate alternatives to postgrey
Make sure class-C ip subnet auto-whitelists are supported

- Allow dynamic mysql lookup tables in recipient checks?

- Templates:
-- Add XML templates
-- Add CSV templates
-- Add alternate styles
-- Add plaintext templates

- test conversion tools for debmin, Vexim, Postfixadmin

- SmtpProxy:
-- Multiple target SMTP servers (fail over, load balancing)

- All:
-- Multiple database servers (fail over, load balancing)*

- Vacation:
-- Test new vacation query

- RFC Notfiy:
-- test rfc notify

- Setup:
-- finish mkadmin.pl/rename to vboxadm.pl

- AWL Cron:
-- test AWL cron with different log formats

- Extensions:
-- Add "customer extension"

- Translation:
-- Translate status messages*

- CGI::Application:
-- add ActionDispatch when Bug #65224 is fixed - test if cgi.fix_pathinfo = 1 solves this issue

- Aliases:
-- allow multiple entries for each alias, i.e. make UNIQUE(local_part,domain_id,goto) and use GROUP_CONCAT

- FastCGI:
-- test/debug FastCGI
-- see http://cgi-app.org/index.cgi?FastCGI
-- see https://docs.google.com/Doc?docid=0AY-OcCA2-lySZGQzNjNmZzlfNzdnYjRoZGg3Yg&hl=en#FastCGI_8880821961767984

- Mailarchive:
-- create a mailarchive using always_bcc and a custom postfix transport
-- mailarchive will be perl script called on demand or delivered to via SMTP/LMTP
-- will store every mail passing through the mailserver, signing it and extracting meta-data
-- use an ARCHIVE table to store the data
-- use one table per month/year ... depending on the amount of traffic
-- naming: archive_YEAR_MONTH
-- optional storage on FS
-- always index metadata in DB, table archive_metadata
-- export option
-- all inserts are logged, logs are signed
-- cron/cleanup will create the necessary tables, delete and optimize the old ones

- Maillog

- Evaluate datatables:
-- http://www.datatables.net/development/server-side/perl_mysql

* - in progress

